<form method="post" action="{{ path('app_collectes_huile_delete', {'id': collectes_huile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collectes_huile.id) }}">
    <button class="btn">Delete</button>
</form>

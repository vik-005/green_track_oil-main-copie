<form method="post" action="{{ path('app_entre_stock_delete', {'id': entre_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entre_stock.id) }}">
    <button class="btn">Delete</button>
</form>

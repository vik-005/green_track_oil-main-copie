<form method="post" action="{{ path('app_stock_delete', {'id': sorties_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sorties_stock.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}StockHuile{% endblock %}

{% block content %}
    <h1>StockHuile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock_huile.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ stock_huile.quantite }}</td>
            </tr>
            <tr>
                <th>DateMaj</th>
                <td>{{ stock_huile.dateMaj ? stock_huile.dateMaj|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stock_huile_index') }}">back to list</a>

    <a href="{{ path('app_stock_huile_edit', {'id': stock_huile.id}) }}">edit</a>

    {{ include('stock_huile/_delete_form.html.twig') }}
{% endblock %}

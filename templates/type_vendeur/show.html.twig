{% extends 'base.html.twig' %}
{% block sidebar %}
{% include "_partials/sidebar/sidebar_home.html.twig" %}
{% endblock %}

{% block title %}TypeVendeur{% endblock %}

{% block content %}
    <h1>TypeVendeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_vendeur.id }}</td>
            </tr>
            <tr>
                <th>NomType</th>
                <td>{{ type_vendeur.nomType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_vendeur_index') }}">back to list</a>

    <a href="{{ path('app_type_vendeur_edit', {'id': type_vendeur.id}) }}" class="btn btn-primary">edit</a>

    {{ include('type_vendeur/_delete_form.html.twig') }}
{% endblock %}

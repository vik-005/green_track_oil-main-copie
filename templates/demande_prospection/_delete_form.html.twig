<form method="post" action="{{ path('app_demande_prospection_delete', {'id': demandes_prospection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandes_prospection.id) }}">
    <button class="btn">Delete</button>
</form>
